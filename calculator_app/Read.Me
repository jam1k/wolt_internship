setting up the flask invironment:
$ pip3 install virtualenv
$ virtualenv env
$ source env/bin/activate

In virtual environment install flask, dateutil, cerberus:
$ pip install -r /path/to/requirements.txt

Run the server:
$python3 calculate.py

I expect input json files here: http://127.0.0.1:5000/read_json
